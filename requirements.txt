# مكتبات بايثون اللازمة لتشغيل المحول
PyMuPDF>=1.24      # قراءة وعرض صفحات الـ PDF
pytesseract>=0.3.10 # واجهة Tesseract للـ OCR (يتطلب تثبيت Tesseract في النظام)
Pillow>=10.0       # معالجة الصور
python-docx>=1.1   # إنشاء ملفات Word (.docx)
arabic-reshaper>=3.0 # (اختياري) معالجة أشكال الحروف العربية
python-bidi>=0.4   # (اختياري) ضبط اتجاه النص ثنائي الاتجاه
